- 采用 Fake 对象模式（如 `FakeModel`, `_FM`, `_FTC`）模拟外部依赖，确保测试在无网络环境下具备确定性和快速执行能力。
- 统一使用 `tmp_path` fixture 作为会话存储和文件操作的根目录，并在测试结束后自动清理，保证环境隔离。
- 在涉及多轮交互的测试中，通过断言 `agent.messages` 的角色序列（如 `["system", "user", "assistant", "tool"]`）来验证状态机的流转正确性。
- 对敏感信息（如 `MU_API_KEY`）进行严格的红action测试，确保评估报告和日志中不包含明文密钥。