- 所有评测任务均遵循“初始化工作区 -> 注入 Prompt -> 执行 Agent -> Validator 校验 -> 生成报告”的标准流水线模式。
- 评测结果统一持久化为 JSON 格式的结构化摘要与 Markdown 格式的可读报告，并同步更新至 `latest-summary` 文件。
- 在涉及真实模型调用的日志记录中，强制对包含 `API_KEY`、`TOKEN` 或 `SECRET` 的环境变量值进行正则脱敏处理。