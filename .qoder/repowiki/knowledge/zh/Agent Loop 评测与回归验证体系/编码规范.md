- 采用“驱动脚本 + 独立工作区”模式，每个评测任务在临时创建的 `workspace` 目录下执行，确保环境隔离。
- 统一使用 `summary.json` 记录结构化评测数据（通过率、耗时、退出码），并同步生成 `summary.md` 供人工查阅。
- 在涉及真实模型调用的输出文件中，实施敏感信息（如 API Key）的红action（Redaction）处理，防止密钥泄露。
- 评测脚本通过检查 `pytest` 退出码及特定文件存在性来自动判定任务是否通过（PASS/FAIL）。