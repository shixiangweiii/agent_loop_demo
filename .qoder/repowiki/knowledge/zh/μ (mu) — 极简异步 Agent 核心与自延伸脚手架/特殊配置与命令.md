- **环境配置**：通过环境变量 `MU_BASE_URL`、`MU_MODEL`、`MU_API_KEY` 配置模型后端，不自动加载 `.env` 文件。
- **启动命令**：`python -m mu "<task>"`（Headless 模式）或 `python -m mu --tui`（交互式终端）。
- **高级特性开关**：`--code`（启用 Native Code-action）、`--permission readonly`（开启权限护栏）、`--sandbox docker`（实验性容器隔离）。