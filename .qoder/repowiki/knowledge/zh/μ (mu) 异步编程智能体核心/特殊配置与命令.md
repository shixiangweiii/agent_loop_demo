- **环境变量配置**：必须设置 `MU_MODEL` 和 `MU_API_KEY` (或 `OPENAI_API_KEY`) 才能运行。
- **运行命令**：
  - Headless 模式：`python -m mu "<task description>"`
  - TUI 模式：`python -m mu --tui`
  - 续跑会话：`python -m mu --resume <SESSION_ID>`
- **扩展开发**：扩展需遵循 `mu.extsdk` 定义的 JSONL 协议，放置在 `.mu/extensions/` 目录下可自动加载。