- 所有工具函数（包括内置和扩展）均返回字符串形式的结果，错误信息也通过字符串返回而非抛出异常，以便模型自我纠错。
- 采用基于能力（Capability-based）的权限策略（`permission.py`），通过 `read`, `write`, `shell`, `code_exec` 等标签而非工具名来控制访问权限。
- 会话历史以树状结构存储于 JSONL 文件中，每个节点包含唯一 ID 和父节点 ID，支持非线性编辑和多分支探索。
- 内核与表现层完全解耦，所有状态变更和动作均通过 `EventEmitter` 发布结构化事件，订阅者负责具体的渲染或统计工作。