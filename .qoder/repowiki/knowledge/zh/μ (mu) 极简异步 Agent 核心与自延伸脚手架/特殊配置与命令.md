- **统一安装与运行**：通过 `pip install -e ".[dev,tui]"` 一次性装配所有依赖，使用 `python -m mu` 及其子命令（如 `--tui`, `.eval`, `.dgm`）触发不同模块功能。
- **环境变量配置**：遵循零依赖原则，不自动加载 `.env`，需通过 `source .env` 显式注入 `MU_BASE_URL` 等变量以适配不同模型后端。