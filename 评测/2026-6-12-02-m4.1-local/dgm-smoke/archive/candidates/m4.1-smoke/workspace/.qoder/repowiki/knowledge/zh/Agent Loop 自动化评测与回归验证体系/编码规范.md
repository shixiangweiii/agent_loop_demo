- 所有评测脚本均定义 `EVAL_DIR` 和 `PROJECT_ROOT` 常量，确保路径解析在不同运行环境下的一致性。
- 统一使用 `Path.write_text` 配合 `encoding="utf-8"` 处理所有中间产物（如 prompt, stdout, validation logs）的持久化。
- 评测结果均采用 JSON 与 Markdown 双格式输出，其中 JSON 用于程序化分析，Markdown 用于人工快速审查。