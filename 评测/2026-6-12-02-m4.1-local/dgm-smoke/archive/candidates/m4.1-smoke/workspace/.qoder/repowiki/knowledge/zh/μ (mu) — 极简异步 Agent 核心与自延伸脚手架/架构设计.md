- **事件总线驱动**：以 `mu.events` 为核心枢纽，解耦智能体循环 (`agent.py`)、工具执行 (`tools.py`) 与前端渲染 (`tui.py`)。
- **沙箱隔离机制**：通过 `environment.py` 和 `codeact.py` 提供安全的代码执行环境，确保自延伸操作在受控子进程中运行。
- **统一评测闭环**：利用 `eval.py` 与 `dgm.py` 构建自动化回归验证体系，将评测结果反馈至会话上下文以实现持续优化。