- 错误即数据：所有工具执行异常均捕获并转换为字符串返回给模型，由模型自主纠错而非中断流程。
- 结构化事件流：所有状态变更（如模型调用、工具执行、扩展加载）均通过 `EventEmitter` 发出结构化事件，禁止在核心逻辑中直接打印。
- JSONL 持久化：会话历史与扩展通信均采用 append-only 的 JSONL 格式，确保 KV-cache 友好性与崩溃恢复能力。
- 能力级权限门控：权限策略基于工具的能力标签（如 write, shell, code_exec）进行判断，而非简单的工具名黑名单。