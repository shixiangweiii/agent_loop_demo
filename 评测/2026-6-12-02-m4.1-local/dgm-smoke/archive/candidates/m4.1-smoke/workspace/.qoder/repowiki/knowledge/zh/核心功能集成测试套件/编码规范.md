- 所有涉及 I/O 或 Agent 运行的测试函数均声明为 `async def`，由框架自动调度执行。
- 使用 `tmp_path` 作为所有文件读写操作的根目录，确保测试结束后无残留且互不干扰。
- 采用“脚本化模型”模式：预先定义 `FakeModel` 返回的消息序列，精确控制 Agent 的多轮交互逻辑。
- 在断言中优先检查业务结果（如文件内容、消息角色序列），而非仅验证函数是否被调用。