- **环境变量**：必须设置 `MU_MODEL` 和 `MU_API_KEY`（或 `OPENAI_API_KEY`）。
- **运行命令**：
  - Headless 模式：`python -m mu "<task>"`
  - TUI 模式：`python -m mu --tui`
  - 启用代码动作：`python -m mu --code`
- **评测运行**：`python -m mu.eval --suite basic`
- **DGM 候选者运行**：`python -m mu.dgm --candidate-dir <path>`