- 所有工具函数和执行逻辑均采用异步优先（async-first）设计，阻塞 IO 通过 `asyncio.to_thread` 或子进程处理。
- 工具执行错误不抛出异常，而是返回包含错误信息的字符串或 `ToolResult`，交由模型自行纠错。
- 模块内部通过 `EventEmitter` 实现同步事件分发，解耦核心逻辑与渲染/监控等副作用。
- 配置项优先从环境变量（如 `MU_MODEL`, `MU_API_KEY`）读取，并在初始化时进行严格校验。