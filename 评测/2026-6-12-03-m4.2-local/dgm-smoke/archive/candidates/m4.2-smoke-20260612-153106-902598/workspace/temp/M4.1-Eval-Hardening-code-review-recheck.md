# M4.1 Eval Hardening 代码评审二次复核

- 复核对象：`temp/M4.1-Eval-Hardening-code-review.md`
- 复核时间：2026-06-12
- 结论：报告中的 P1、P2-1、P2-2、P2-3 均为真实问题或真实覆盖缺口，已采纳并修复；P3 中低风险项择要清理。

## 逐项结论

| 编号 | 复核判断 | 处理 |
|---|---|---|
| P1-1 `redact_secrets` 短值误替换 | 真实。`TOKENIZERS_PARALLELISM=false` 会把产物中所有 `false` 脱敏，可能污染 summary/report。 | 已修：secret env value 增加最小长度阈值，短值不参与 redact/scan；补 `test_redact_secrets_ignores_short_non_secret_values`。 |
| P2-1 workspace 文件 secret scan 盲点 | 真实。旧实现会整体忽略 `workspace`，agent 写入真实 env secret 时 scan 仍 PASS。 | 已修：workspace 中 `sk-...` fixture 仍忽略，但若命中真实运行时 env secret 精确值则 FAIL；补 `test_secret_scan_checks_workspace_for_exact_env_secret_only`；README/roadmap 同步范围说明。 |
| P2-2 `mu.eval_gate` 零自动化测试 | 真实覆盖缺口。 | 已补 `tests/test_eval_gate.py`：覆盖 `default_gate_dir`、report 渲染、fake agent 三任务 validator、fixed run-root 复跑、`OSError` 命令失败归档。 |
| P2-3 fixed `--run-root` 复跑失败 | 真实。固定 `candidate_id=m4.1-smoke` 导致第二次 DGM smoke candidate workspace 已存在。 | 已修：DGM smoke candidate id 加微秒时间戳；补 fixed run-root 重复运行测试。 |
| P3-1 eval summary 双写缺注释 | 真实但低风险。 | 已补注释，说明先写 summary 是为了把 summary 自身纳入 scan。 |
| P3-2 `_failure_stage` 冗余分支 | 真实但低风险。 | 已简化为 `agent_returncode != 0`。 |
| P3-3 gate 目录月/日未零填充 | 真实但低风险。 | 已改为 `%Y-%m-%d-%H%M%S`；补固定时间测试。 |
| P3-4 `_run` 只捕获 timeout | 真实但低概率。 | 已捕获 `OSError`，归档为 rc=127；补测试。 |
| P3-5 独立 `mu.eval` 的 parent latest-summary 不在 per-run scan 内 | 真实边界，但风险低：latest-summary 来自已脱敏 summary 副本；gate 整树扫描会覆盖。 | 暂不改，避免单次 eval 扫描历史 run_root 造成旧产物误伤。 |
| P3-6 同文件 env + pattern 双 hit | 非功能 bug。 | 保留现状。 |

## 修复后验证

- `./.venv/bin/python -m pytest -q tests/test_eval.py tests/test_eval_gate.py tests/test_dgm.py`：`22 passed`
- `./.venv/bin/python -m pytest -q`：`94 passed, 1 skipped`
