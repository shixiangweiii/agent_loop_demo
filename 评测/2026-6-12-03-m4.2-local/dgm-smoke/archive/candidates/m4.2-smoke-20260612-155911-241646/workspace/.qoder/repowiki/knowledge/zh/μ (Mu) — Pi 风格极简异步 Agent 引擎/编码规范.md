- 错误处理字符串化：所有工具调用异常均捕获并转为字符串返回，交由模型自行纠错而非中断流程。
- 异步优先原则：从 M0 阶段起即全面采用 asyncio，包括文件 IO (`to_thread`) 和 Bash 执行 (`create_subprocess_shell`)。
- Append-only 持久化：Session 消息以 JSONL 格式追加存储，通过 `parent_id` 构建树状结构以支持分支与回溯。
- 能力导向权限：权限策略基于工具的能力标签（如 `write`, `shell`）进行拦截，而非简单的工具名黑名单。